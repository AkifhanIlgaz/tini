// Package icons holds small, stateless SVG icon components shared across
// features and internal/shared/components. Sizing/color is left to the
// caller's CSS (e.g. HeroUI's button/toggle-button components size their
// own <svg> children automatically) rather than baked in here.
package icons
